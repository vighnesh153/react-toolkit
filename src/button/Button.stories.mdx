import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { Button } from './Button';
import { Box, MuiThemeProvider } from '@material-ui/core';
import theme from '../theme';

<Meta
  title="Components/Button"
  component={Button}
/>

<style>{`

`}</style>


# Button Component

Read more about using the base Material UI button [here](https://material-ui.com/components/buttons).

### Import
```tsx
import { Box, MuiThemeProvider } from '@material-ui/core';
import { Button, ButtonProps, theme } from "@vighnesh153/react-toolkit";
```

### Primary Button
<Canvas>
  <MuiThemeProvider theme={theme}>
    <Box display={'flex'}>
      <Button
        rvLabel={'Primary'}
        rvColor={'primary'}
      />
      <Box ml={2}>
        <Button
          rvLabel={'Disabled'}
          rvColor={'primary'}
          disabled
        />
      </Box>
    </Box>
  </MuiThemeProvider>
</Canvas>


### Secondary Button
<Canvas>
  <MuiThemeProvider theme={theme}>
    <Box display={'flex'}>
      <Button
        rvLabel={'Secondary'}
        rvColor={'primary'}
        variant={'outlined'}
      />
      <Box ml={2}>
        <Button
          rvLabel={'Disabled'}
          variant={'outlined'}
          disabled
        />
      </Box>
    </Box>
  </MuiThemeProvider>
</Canvas>


### Success Button
<Canvas key={3}>
  <Button
    rvLabel={'Success'}
    rvColor={'success'}
  />
</Canvas>

### Info Button
<Canvas key={4}>
  <Button
    rvLabel={'Info'}
    rvColor={'info'}
  />
</Canvas>

### Warning Button
<Canvas key={5}>
  <Button
    rvLabel={'Warning'}
    rvColor={'warning'}
  />
</Canvas>

### Error Button
<Canvas key={6}>
  <Button
    rvLabel={'Error'}
    rvColor={'error'}
  />
</Canvas>
